[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "skimindex"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "plumbum>=1.8.0",
]

[project.scripts]
download = "skimindex._download:main"
